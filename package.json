{
  "name": "caledor-tour",
  "private": true,
  "scripts": {
    "start": "node server/server.js",
    "stop": "powershell -Command \"$p = (Get-NetTCPConnection -LocalPort 3456 -ErrorAction SilentlyContinue | Select-Object -First 1).OwningProcess; if ($p) { Stop-Process -Id $p -Force; Write-Host 'Stopped server on port 3456' } else { Write-Host 'No server running on port 3456' }\"",
    "restart": "npm run stop && npm start",
    "seed": "node server/seed.js",
    "import-media": "node server/scripts/import-media.js"
  }
}
